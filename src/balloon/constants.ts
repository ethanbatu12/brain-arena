/** Tuning knobs for the Balloon Order Challenge sprint. */
export const BALLOON_GAME_MS = 60_000; // 60-second sprint, same as the other games

export const POINTS_PER_BALLOON = 25;
export const BONUS_EVERY_SETS = 5; // every Nth completed set in a row earns a bonus
export const BONUS_POINTS = 25;

export const MIN_LEVEL = 1;
export const MAX_LEVEL = 10;

/** Balloon count per set grows from MIN_BALLOONS (level 1) to MAX_BALLOONS (level 10). */
export const MIN_BALLOONS = 4;
export const MAX_BALLOONS = 10;
